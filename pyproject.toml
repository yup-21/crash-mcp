[project]
name = "crash-mcp"
version = "0.1.0"
description = "MCP server for crash dump analysis"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"

dependencies = [
    "mcp",
    "python-dotenv",
    "pexpect",
    "drgn",
    "click",
    "pyyaml",
]

[project.optional-dependencies]
kb = [
    "chromadb",
    "sentence-transformers",
]



[project.scripts]
crash-mcp = "crash_mcp.server:main"
compile-crash = "crash_mcp.compile_crash:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
  "src/crash_mcp/resource/*",
  "knowledge/scripts/drgn/*.py",
  "knowledge/methods/*.yaml",
]

[tool.hatch.build.targets.wheel]
packages = ["src/crash_mcp"]
